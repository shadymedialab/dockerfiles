x-workspace-source: &WORKSPACE_SOURCE "~/ws"

services:
  ws:
    build:
      context: .
      dockerfile: Dockerfile
    ipc: host
    tty: true
    privileged: true
    environment:
      TERM: screen-256color
      LANG: C.UTF-8
      DISPLAY: $DISPLAY
      NVIDIA_DRIVER_CAPABILITIES: all
    volumes:
      - type: bind
        source: /tmp/.X11-unix
        target: /tmp/.X11-unix
      - type: bind
        source: ~/dockerfiles/scripts/install_vscode_extensions.sh
        target: /home/user/install_vscode_extensions.sh
      - type: bind
        source: *WORKSPACE_SOURCE
        target: /home/user/ws
      - type: volume
        source: apt-cache-apt-lists
        target: /var/lib/apt/lists
      - type: volume
        source: apt-cache-apt-archives
        target: /var/cache/apt/archives
    working_dir: /home/user/ws
    command: /bin/bash -c "if [[ -n $${GIT_USER_NAME} && -n $${GIT_USER_EMAIL} ]]; then git config --global user.name $${GIT_USER_NAME} && git config --global user.email $${GIT_USER_EMAIL}; fi && sudo apt update && bash"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]

volumes:
  apt-cache-apt-lists:
    name: ubuntu20.04-apt-cache-apt-lists
  apt-cache-apt-archives:
    name: ubuntu20.04-apt-cache-apt-archives
